# hotlist-crawler 依赖包
# 通用热榜内容爬虫包，支持知乎、微博、微信公众号等平台的内容抓取

# ===================================================================
# 核心依赖（必需）
# ===================================================================

# --- Web 框架 ---
# FastAPI 用于提供 API 服务（可选，如果只用命令行可不安装）
fastapi>=0.104.1
uvicorn[standard]>=0.24.0

# --- 数据验证 ---
# pydantic 2.10+ 与 pydantic-settings 2.6.1 存在兼容性问题
pydantic>=2.5.0,<2.10
pydantic-settings>=2.1.0

# --- 浏览器自动化 ---
# 核心依赖，用于知乎、微博、微信、抖音等平台
playwright>=1.40.0

# --- HTTP 请求 ---
# 异步 HTTP 客户端
httpx>=0.25.0

# --- 网页解析 ---
# BeautifulSoup4 用于 HTML 解析
beautifulsoup4>=4.12.0
# lxml 作为 BeautifulSoup 的解析器（速度快）
lxml>=4.9.0

# --- 文件类型检测 ---
# 用于自动识别图片格式（GIF/PNG/JPG等）
filetype>=1.2.0

# --- 缓存系统 ---
# Redis 用于缓存抓取结果（可选）
redis>=5.0.0

# --- 环境配置 ---
# 用于加载 .env 配置文件
python-dotenv>=1.0.0

# --- 日志系统 ---
# 统一的日志输出
loguru>=0.7.0

# --- 小红书支持 ---
# 用于小红书加密算法
PyExecJS>=1.5.1

# ===================================================================
# 可选依赖
# ===================================================================

# --- 开发工具（开发时使用）---
# pytest>=7.4.0
# pytest-asyncio>=0.21.0
# black>=23.0.0
# ruff>=0.1.0